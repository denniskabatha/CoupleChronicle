# In app.py, change this line
app.secret_key = os.environ.get("SESSION_SECRET", "dev-secret-key")

# To this line (for development only, not for production)
app.secret_key = "dev-secret-key-for-testing-only"